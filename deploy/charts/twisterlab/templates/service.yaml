apiVersion: v1
kind: Service
metadata:
  name: {{ include "twisterlab.fullname" . }}
  labels:
    {{- include "twisterlab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "twisterlab.selectorLabels" . | nindent 4 }}
